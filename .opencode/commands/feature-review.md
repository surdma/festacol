---
description: Run end-to-end acceptance of a delivered Festacol feature with live evidence
agent: festacol-orchestrator
---

Follow [`.agents/commands/feature-review.md`](../../.agents/commands/feature-review.md). Map each acceptance criterion to observable behavior, exercise the real end-to-end flow against the real server path, cover complete UI and server states, run the global `/dogfood` acceptance pass, and return coverage, evidence, and a final pass/block decision in the standard handoff envelope.
