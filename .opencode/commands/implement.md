---
description: Implement an accepted Festacol task with full verification and dogfood evidence
agent: festacol-orchestrator
---

Follow [`.agents/commands/implement.md`](../../.agents/commands/implement.md). Route to the smallest correct specialist set, implement the smallest complete solution inside each ownership boundary, verify with the repository gates and real runtime behavior, run the global `/dogfood` acceptance pass when a runnable surface exists, and iterate on failures before returning the standard handoff envelope for reviewer verdict.
